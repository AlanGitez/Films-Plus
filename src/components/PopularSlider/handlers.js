export const handlerHover = (e) => {
    // const root = document.querySelector("#root");
    // const movie = e.target.className && document.querySelector(`div.slider > div.${e.target.className}`)
    // console.log(movie);
    // document.body.appendChild(movie)
};

